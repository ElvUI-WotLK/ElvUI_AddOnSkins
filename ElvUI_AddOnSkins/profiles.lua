local addonName = ...;
local E, L, V, P, G, _ = unpack(ElvUI);

P.addOnSkins = {
	embed = {
		single = true,
		dual = false,
		main = "Recount",
		left = "Recount",
		right = "Recount",
		rightChat = false,
		leftWidth = 200,
		belowTop = false,
	}
};